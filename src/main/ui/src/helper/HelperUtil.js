export default function getFullUrl(uriPath) {
 // let host = window.location.port === '3000' ? 'http://localhost:8080' : '';
 // let finalUrl = host + uriPath
 // console.debug('Final URL', finalUrl);
  return uriPath;
}
